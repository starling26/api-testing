# Azure DevOps Pipeline for Playwright API Tests with Allure Reporting
# Author: Starling De La Cruz
# Project: DummyJSON API Testing Framework

trigger:
  branches:
    include:
      - main
      - master
      - develop
  paths:
    exclude:
      - README.md
      - docs/*

pr:
  branches:
    include:
      - main
      - master

variables:
  nodeVersion: '20.x'
  npmCache: $(Pipeline.Workspace)/.npm

stages:
  - stage: Test
    displayName: 'Run API Tests'
    jobs:
      - job: APITests
        displayName: 'Execute Playwright Tests'
        pool:
          vmImage: 'ubuntu-latest'
        timeoutInMinutes: 30
        steps:
          - task: NodeTool@0
            displayName: 'Install Node.js $(nodeVersion)'
            inputs:
              versionSpec: '$(nodeVersion)'

          - task: Cache@2
            displayName: 'Cache npm packages'
            inputs:
              key: 'npm | "$(Agent.OS)" | package-lock.json'
              restoreKeys: |
                npm | "$(Agent.OS)"
              path: $(npmCache)

          - script: npm ci --cache $(npmCache)
            displayName: 'Install dependencies'

          - script: npx playwright test --reporter=list,html,allure-playwright
            displayName: 'Run API Tests'
            continueOnError: true
            env:
              CI: 'true'

          - script: |
              npm install -g allure-commandline
              allure generate allure-results --clean -o allure-report || true
            displayName: 'Generate Allure Report'
            condition: always()

          - task: PublishTestResults@2
            displayName: 'Publish Test Results'
            condition: always()
            inputs:
              testResultsFormat: 'JUnit'
              testResultsFiles: '**/test-results/**/*.xml'
              mergeTestResults: true
              failTaskOnFailedTests: false
              testRunTitle: 'API Tests - $(Build.BuildNumber)'

          - task: PublishPipelineArtifact@1
            displayName: 'Publish Playwright Report'
            condition: always()
            continueOnError: true
            inputs:
              targetPath: 'playwright-report'
              artifact: 'playwright-report'

          - task: PublishPipelineArtifact@1
            displayName: 'Publish Allure Results'
            condition: always()
            continueOnError: true
            inputs:
              targetPath: 'allure-results'
              artifact: 'allure-results'

          - task: PublishPipelineArtifact@1
            displayName: 'Publish Allure Report'
            condition: always()
            continueOnError: true
            inputs:
              targetPath: 'allure-report'
              artifact: 'allure-report'